apiVersion: platform.example.com/v1alpha1
kind: XApplication
metadata:
  name: nginx-app
  namespace: apps
spec:
  appName: nginx-app
  image: nginx:1.21
  replicas: 2
  targetCluster: workload-cluster-1
