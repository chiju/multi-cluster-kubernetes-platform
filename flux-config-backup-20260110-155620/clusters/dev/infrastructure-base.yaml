apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-base
  namespace: flux-system
spec:
  interval: 5s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./flux-config/infrastructure-base
  prune: true
  wait: true
